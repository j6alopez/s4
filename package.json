{
  "devDependencies": {
    "@types/node": "20.8.2",
    "@types/node-fetch": "^2.6.10",
    "gts": "^5.2.0",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "bootstrap": "^5.3.2"
  }
}
